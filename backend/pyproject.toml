[project]
name = "hermes-guide-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "lightrag-hku",
    "google-genai",
    "numpy",
    "slowapi",
    "structlog",
    "pydantic>=2",
]
